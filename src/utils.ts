import type { PlaceHolderProps } from "./index";

/**
 * Generate a data URL for an SVG placeholder image.
 * Can be used as the src for <img> or <Image> (Next.js).
 */
export { generateSvgDataUrl } from "./index";

/**
 * Types for the placeholder props.
 */
export type { PlaceHolderProps };
